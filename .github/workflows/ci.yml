name: backoffice-ci

run-name: ${{ github.actor }}

on: [push]
jobs:

  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: test
        run: bash ../../backoffice/scripts/run_bg.sh